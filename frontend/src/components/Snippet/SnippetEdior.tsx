import { useQuery } from "@apollo/client";

import { QUERY_SNIPPET } from "../../operations/Snippet";

import styles from "./Snippet.module.css";

type SnippetProps = {

}

export const SnippetEditor = ({}: SnippetProps) => {
  return <></>;
};
